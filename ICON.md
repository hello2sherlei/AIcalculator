# 图标说明

## 需要的图标文件

为了应用能够正常显示托盘图标和打包图标，你需要准备以下文件：

1. **icon.png** - 托盘图标（建议尺寸：256x256 像素）
2. **icon.ico** - Windows 图标（用于打包）
3. **icon.icns** - macOS 图标（用于打包）

## 临时解决方案

应用在没有图标的情况下仍然可以运行，只是托盘图标会显示为默认图标。

## 创建图标

你可以：

1. 使用在线工具（如 https://www.favicon-generator.org/）从PNG生成ICO和ICNS
2. 设计一个简单的时钟或计时器图标
3. 使用现成的免费图标资源

## 推荐图标样式

- 简约的时钟或计时器图标
- 科技风格的数字或计数器
- 蓝色/青色配色（与应用主题一致）
