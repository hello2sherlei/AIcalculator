# 图标说明

## 菜单栏/托盘图标

应用已内置了一个**白色线条时钟图标**作为默认菜单栏图标，因此**即使没有自定义图标，应用也能正常显示图标**。

### 图标加载优先级

1. **优先加载**: 项目根目录的 `icon.png`（如果存在）
2. **备用方案**: 内置的白色线条时钟图标（SVG生成）

### 内置图标特性

- ✓ 简洁的白色线条圆表盘设计
- ✓ 适配macOS菜单栏（22x22像素）
- ✓ 自动适配深色/浅色模式（Template Image）
- ✓ 不会出现空图标或缺失图标的情况

## 自定义图标（可选）

如果你想使用自定义图标，可以准备以下文件：

1. **icon.png** - 菜单栏/托盘图标
   - 推荐尺寸：256x256 像素（应用会自动调整为22x22）
   - 格式：PNG（支持透明背景）
   - 建议：单色/黑白图标，适合菜单栏显示

2. **icon.ico** - Windows 应用图标（用于打包）
3. **icon.icns** - macOS 应用图标（用于打包）

## 创建图标

你可以：

1. 使用在线工具（如 https://www.favicon-generator.org/）从PNG生成ICO和ICNS
2. 设计一个简单的时钟或计时器图标
3. 使用现成的免费图标资源

## 推荐图标样式

- 简约的时钟或计时器图标
- 科技风格的数字或计数器
- 蓝色/青色配色（与应用主题一致）
